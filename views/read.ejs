<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>READ all Cars</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="bg-zinc-800 h-full w-full min-h-screen">
        <div class="nav text-zinc-300 mt-[15] mb-[15] gap-7 flex">
        <a href="/create" class="text-amber-300 hover:text-amber-400 mt-[15]">Create Car  </a>
        <a href="/read" class="text-amber-300 hover:text-amber-400 mt-[15]">Read Cars</a>
    </div>
    <h1 class="text-white text-3xl font-bold text-center">READ all Cars</h1>
    <% if (Cars.length > 0) { %>
        <div class="Cars flex flex-wrap gap-10">
        <% Cars.forEach(Car => { %>
        <div class="Car flex flex-col gap-5 mt-5 bg-zinc-700 p-5 rounded-lg w-1/6">
            <div class="pic mx-auto bg-zinc-500 w-36 h-26 rounded-lg">
                <img src="<%= Car.imageurl %>" alt="Car image" class="w-full h-full object-cover rounded-lg">
            </div>
            <div class="name mx-auto text-gray-400"><%= Car.name %></div>
            <div class="email mx-auto text-gray-400"><%= Car.email %></div>
            <div class="flex gap-5 mx-auto">
                <a href="/edit/<%= Car._id %>" class=" text-amber-300 py-2 px-4 ">Edit</a>
                <a href="/delete/<%= Car._id %>" class=" text-red-400 py-2 px-4 ">Delete</a>
            </div>
        </div>
        <% }); %>
    </div>
        <%} else { %>
            <p class="text-zinc-300 mt-5">No Cars found.</p>
        <%}%>
    </div>
    </div>
</body>
</html>